1. Сделать карусель, в которой в правом верхнем углу будет предпросмотр следующего слайда.
Для последнего слайда это должен быть предпросмотр первого слайда.
Советую посмотреть на bxSlider.


2. Сделать выпадающий список (select), где каждая опция содержит иконку (например, список соц.сетей).


3. Сделать табы без плагина. Активация должна происходить примерно так:
initTabs($('.block-tabs'));
function initTabs($holder) {
    $holder.each(function(tab) {
        var $tab = $(tab);

        // тут происходит расвешивание событий на нужные блоки в каждом табе
        // таким образом можно будет добавлять несколько блоков на страницу и они
        // будут работать независимо друг от друга
    });
}
В этом задании нужно сначала разметить всю структуру кода комментариями, а потом только писать рабочий код.

